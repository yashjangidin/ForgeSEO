import type { ReactElement } from "react";

export const Analytics = (): ReactElement => (
  <main className="mx-auto max-w-7xl px-4 py-8">
    <h1 className="text-3xl font-semibold text-ink">Analytics</h1>
    <p className="mt-4 rounded border border-slate-200 bg-white p-4 text-slate-700">
      Connect Google Analytics to start collecting analytics.
    </p>
  </main>
);
